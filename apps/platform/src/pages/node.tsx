import { HomeLayout } from '@/modules/layout/home-layout';
import { NodeComponent } from '@/modules/node';

const NodePage = () => {
  return (
    <HomeLayout>
      <NodeComponent />
    </HomeLayout>
  );
};

export default NodePage;
