export interface DataItem {
  label: string;
  pva: number;
  bad_rate: number;
  avg_score: number;
  positive_rate: number;
}

export interface ChartDataItem {
  label: string;
  value: number;
  type: string;
}
